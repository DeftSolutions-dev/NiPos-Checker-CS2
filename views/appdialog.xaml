<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="NiposChecker.Views.AppDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="" SizeToContent="Height" Width="400" WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Background="#00FFFFFF" AllowsTransparency="True" ShowInTaskbar="False" xmlns:views="clr-namespace:NiposChecker.Views;assembly=NiposChecker" MouseLeftButtonDown="Window_MouseLeftButtonDown">
  <Border CornerRadius="14" Background="{StaticResource Brush_Panel}" BorderThickness="1" BorderBrush="{StaticResource Brush_Line2}" ClipToBounds="True">
    <UIElement.Effect>
      <DropShadowEffect Color="#FF000000" BlurRadius="40" ShadowDepth="12" Opacity="0.6" />
    </UIElement.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <Border Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="{StaticResource Brush_Line}" Padding="18,16">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
          </Grid.ColumnDefinitions>
          <TextBlock Name="HeaderTitle" Grid.Column="0" Text="" FontFamily="{StaticResource Font_Sans}" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource Brush_Text}" VerticalAlignment="Center" />
          <Button Grid.Column="1" Style="{StaticResource WindowCloseButton}" Width="30" Height="30" Click="No_Click">
            <Viewbox Width="15" Height="15">
              <Canvas Width="24" Height="24">
                <Path Data="M6,6 L18,18 M18,6 L6,18" StrokeThickness="1.7" StrokeStartLineCap="Round" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" />
              </Canvas>
            </Viewbox>
          </Button>
        </Grid>
      </Border>
      <Grid Row="1" Margin="18,20,18,18">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Name="IconBorder" Grid.Column="0" Width="42" Height="42" CornerRadius="11" VerticalAlignment="Top" Margin="0,0,15,0" BorderThickness="1">
          <Viewbox Width="20" Height="20">
            <Canvas Width="24" Height="24">
              <Path Name="IconPath" StrokeThickness="1.7" StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" />
            </Canvas>
          </Viewbox>
        </Border>
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
          <TextBlock Name="Heading" FontFamily="{StaticResource Font_Sans}" FontSize="14.5" FontWeight="SemiBold" Foreground="{StaticResource Brush_Text}" TextWrapping="Wrap" />
          <TextBlock Name="Message" FontSize="12.5" Foreground="{StaticResource Brush_Dim}" TextWrapping="Wrap" Margin="0,6,0,0" LineHeight="18" Visibility="Collapsed" />
        </StackPanel>
      </Grid>
      <Border Grid.Row="2" BorderThickness="0,1,0,0" BorderBrush="{StaticResource Brush_Line}" Padding="18,14">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Name="NoBtn" Style="{StaticResource GhostBtn}" Padding="18,0" Margin="0,0,9,0" Click="No_Click">
            <TextBlock Name="NoText" Text="Нет" VerticalAlignment="Center" />
          </Button>
          <Button Name="YesBtn" Style="{StaticResource PrimaryBtn}" Padding="18,0" Click="Yes_Click">
            <TextBlock Name="YesText" Text="Да" VerticalAlignment="Center" />
          </Button>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</Window>